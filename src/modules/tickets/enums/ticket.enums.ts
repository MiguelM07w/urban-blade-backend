export enum PaymentMethod {
  EFECTIVO = 'efectivo',
  TARJETA = 'tarjeta',
  // Futuro: TRANSFERENCIA = 'transferencia'
}

export enum PaymentStatus {
  PENDIENTE = 'pendiente',
  PAGADO = 'pagado',
}
